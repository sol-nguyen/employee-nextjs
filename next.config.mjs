/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "example.com"], // Add the domains of your image providers here
  },
};

export default nextConfig;
